/**
 * Hundred Steps to Life — Field Notes design reminder:
 * This shell intentionally stays minimal so the field-journal learning flow,
 * not framework chrome, owns the user’s attention.
 */
import Home from "./pages/Home";

export default function App() {
  return <Home />;
}
